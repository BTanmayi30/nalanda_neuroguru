[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mypack"
version = "0.1.0"
description = "My custom math utility package"
authors = [
    {name = "M Prasad"}
]
readme = "README.md"
requires-python = ">=3.7"
